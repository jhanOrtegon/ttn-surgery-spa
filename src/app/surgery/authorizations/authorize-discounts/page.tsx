'use client';

import { GeneralLayout } from '@/components/layout';
import React from 'react';

const AuthorizeDiscounts = () => {
	return (
		<GeneralLayout title='AuthorizeDiscounts'>
			<p>AuthorizeDiscounts</p>
		</GeneralLayout>
	);
};

export default AuthorizeDiscounts;
